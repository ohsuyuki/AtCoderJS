function getApplePieTaste(apples) {
    return 0;
}

const N = 5;
const L = 2;

let apples = [];
for(const i = 0; i < N; i++) {
    apples.push(L + i + 1);
}

// let min = getAbsMin();

let originalTaste = getApplePieTaste(apples);

let applePies = []
for(const i = 0; i< N; i++) {
    let
}